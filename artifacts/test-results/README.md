# Preserved test evidence

`unit-tests-python-3.12.txt` records the successful CPU-compatible unit and
contract test run for the research-hardening change. The GitHub Actions
workflow repeats the same discovery command on every push and pull request.

The log is evidence of one run, not a substitute for rerunning the tests.
Its SHA-256 is recorded in the accompanying `sha256.txt` file.
